apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/proxy-connect-timeout: '5'
  name: soc-web
spec:
  rules:
    - host: soc-web.example.com
      http:
        paths:
          - backend:
              service:
                name: soc-web
                port:
                  number: 3002
            path: /
            pathType: ImplementationSpecific
  tls:
    - secretName: soc-web-tls
